import styled from "styled-components";

export const ContainerSC = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 1rem;
`;

export const CardUlSC = styled.ul`
  padding: 1rem;
  border-radius: 0.5rem;
  box-shadow: 2px 2px 5px 2px rgba(0, 0, 0, 0.1);
  background: white;
`;
